# ICTU-Ex Architecture Diagram

## High-Level System Architecture

```
┌──────────────────────────────────────────────────────────────────────┐
│                      ICTU-Ex Backend System                          │
│                   Service-Oriented + Event-Driven                    │
├──────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                     CLIENT LAYER                               │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐    │  │
│  │  │ Android App  │  │  Web Client  │  │  Other Services  │    │  │
│  │  └──────┬───────┘  └──────┬───────┘  └────────┬─────────┘    │  │
│  │         │ REST API         │                   │              │  │
│  │         └─────────┬────────┴───────────────────┘              │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                            │                                         │
│                    HTTP/JSON (REST)                                 │
│                            ▼                                         │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                    SERVICE LAYER                               │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │                                                                 │  │
│  │  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐       │  │
│  │  │    AUTH      │   │   LISTING    │   │  MESSAGING   │       │  │
│  │  │  Service     │   │  Service     │   │  Service     │       │  │
│  │  │              │   │              │   │              │       │  │
│  │  │ - Register   │   │ - Create     │   │ - Conversations    │  │
│  │  │ - Login      │   │   Listings   │   │ - Send Messages    │  │
│  │  │ - Verify     │   │ - Search     │   │ - Retrieve Chat    │  │
│  │  │   Email      │   │ - Image      │   │                    │  │
│  │  │              │   │   Upload     │   │                    │  │
│  │  └──────┬───────┘   └──────┬───────┘   └────────┬─────────┘  │  │
│  │         │                  │                    │             │  │
│  │  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐       │  │
│  │  │NOTIFICATION  │   │    SYNC      │   │   SHARED     │       │  │
│  │  │  Service     │   │  Service     │   │   Module     │       │  │
│  │  │              │   │              │   │              │       │  │
│  │  │ - Email      │   │ - Track      │   │ - Events     │       │  │
│  │  │   Alerts     │   │   Operations │   │ - DTOs       │       │  │
│  │  │              │   │ - Sync       │   │ - Config     │       │  │
│  │  │              │   │   Records    │   │              │       │  │
│  │  └──────┬───────┘   └──────┬───────┘   └──────────────┘       │  │
│  │         │                  │                                  │  │
│  │         └──────────┬───────┴────────────┐                    │  │
│  │                    │ Kafka Events       │                    │  │
│  └────────────────────┼──────────────────────────────────────────┘  │
│                       ▼                                               │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                  MESSAGE BUS (KAFKA)                            │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │                                                                 │  │
│  │  ┌─────────────────────┐  ┌─────────────────────┐             │  │
│  │  │  User Events        │  │  Listing Events     │             │  │
│  │  ├─────────────────────┤  ├─────────────────────┤             │  │
│  │  │ user.verified       │  │ product.posted      │             │  │
│  │  │ user.registration   │  │ image.upload.req    │             │  │
│  │  │ code.generated      │  └─────────────────────┘             │  │
│  │  └─────────────────────┘                                       │  │
│  │         │ ▲                                                    │  │
│  │         │ │         ┌─────────────────────┐                   │  │
│  │         │ │         │ message.sent        │                   │  │
│  │         │ │         └─────────────────────┘                   │  │
│  │         │ │                 │ ▲                               │  │
│  │         └─┼─────────────────┼─┼────────┘                      │  │
│  │           │                 │ │                              │  │
│  └───────────┼─────────────────┼─┼──────────────────────────────┘  │
│              ▼                 ▼ │                                  │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                 DATA LAYER (PERSISTENCE)                       │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │                                                                 │  │
│  │  ┌─────────────────────────────┐  ┌──────────────────────┐   │  │
│  │  │  PostgreSQL (Shared DB)     │  │  Redis (Cache)       │   │  │
│  │  ├─────────────────────────────┤  ├──────────────────────┤   │  │
│  │  │ Database: ictuex            │  │ Session Store        │   │  │
│  │  ├─────────────────────────────┤  │ Listing Cache        │   │  │
│  │  │ Tables:                     │  │ Token Blacklist      │   │  │
│  │  │ • users (auth)              │  └──────────────────────┘   │  │
│  │  │ • listings (listing)        │                             │  │
│  │  │ • messages (messaging)      │                             │  │
│  │  │ • conversations (messaging) │                             │  │
│  │  │ • sync_records (sync)       │                             │  │
│  │  │                             │                             │  │
│  │  │ Features:                   │                             │  │
│  │  │ • Flyway migrations         │                             │  │
│  │  │ • ACID transactions         │                             │  │
│  │  │ • Full-text search          │                             │  │
│  │  └─────────────────────────────┘                             │  │
│  │                                                                 │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
```

---

## Kafka Event Flow: User Registration to Welcome Email

```
User                  Auth Service              Kafka              Notification Service
  │                        │                      │                        │
  ├─────── Register ────────>│                      │                        │
  │         (name, email,    │                      │                        │
  │          password)       │                      │                        │
  │                         │                      │                        │
  │                    Save to DB                  │                        │
  │                    Generate Code               │                        │
  │                         │                      │                        │
  │                    Publish Event ─ VERIFICATION_CODE_GENERATED ─────>│
  │                         │                      │  (email, code)      │
  │                    Return Token                │                   Send Email
  │<───────── JWT ──────────│                      │                        │
  │                         │                      │                        │
  │                         │                      │                   User receives
  │                         │                      │                   verification
  │ Enter Code              │                      │                      email
  │─────── Verify ──────────>│                      │                        │
  │                         │                      │                        │
  │                    Verify Code                 │                        │
  │                    Save Verified=true          │                        │
  │                         │                      │                        │
  │                    Publish Event ─────── USER_VERIFIED ──────────────>│
  │                         │                      │  (email, name)      │
  │                    Return 200 OK               │                   Send Welcome
  │<───────── 200 ─────────│                      │                      Email
  │                         │                      │                        │
  └                         └                      └                        └
```

---

## Module Dependency Graph

```
ictu-ex-app (entry point)
    │
    ├── shared (all modules depend on this)
    │   ├── Kafka config & EventPublisher
    │   ├── Domain events (UserVerifiedEvent, MessageSentEvent, etc.)
    │   ├── Common DTOs & exceptions
    │   └── Redis configuration
    │
    ├── auth
    │   ├── Public API: AuthService, AuthUser
    │   ├── Internal: AuthServiceImpl, UserEntity, UserRepository
    │   ├── Security: JWT, Spring Security config
    │   └── Publishes: VERIFICATION_CODE_GENERATED, USER_VERIFIED
    │
    ├── listing
    │   ├── Public API: ListingService, Listing
    │   ├── Internal: ListingServiceImpl, ListingEntity
    │   ├── Image handling: CloudinaryService, ImageUploadConsumer
    │   └── Publishes: PRODUCT_POSTED, IMAGE_UPLOAD_REQUESTED
    │
    ├── messaging
    │   ├── Public API: MessagingService
    │   ├── Internal: MessagingServiceImpl, MessageEntity
    │   ├── Conversations & Messages entities
    │   └── Publishes: MESSAGE_SENT
    │
    ├── notification
    │   ├── Email service implementation
    │   ├── Consumes: VERIFICATION_CODE_GENERATED, USER_VERIFIED, MESSAGE_SENT
    │   └── Sends emails via configured SMTP
    │
    └── sync
        ├── Sync ledger tracking
        ├── SyncEventConsumer
        ├── Consumes: All domain events (USER_VERIFIED, PRODUCT_POSTED, MESSAGE_SENT, etc.)
        └── Stores events in sync_records table
```

---

## Data Flow: Send Message (Event-Driven)

```
┌─ CLIENT ─────────────────────────────────────────────────────────┐
│                                                                  │
│  POST /messaging/{conversationId}/send                          │
│  { "content": "Hi, is this still available?" }                  │
│                                                                  │
└────────────────────┬─────────────────────────────────────────────┘
                     │ REST API (JSON)
                     ▼
        ┌───────────────────────────┐
        │  MESSAGING SERVICE        │
        ├───────────────────────────┤
        │ 1. Validate message       │
        │ 2. Save to DB             │
        │ 3. Get sender/receiver    │
        │    info from auth DB      │
        │ 4. Publish to Kafka       │
        └────────┬──────────────────┘
                 │
         KafkaTemplate.send()
                 │
         ┌───────┴──────────┬──────────────┐
         │                  │              │
         ▼                  ▼              ▼
    Kafka Topic         Kafka Topic    Kafka Topic
    message.sent        message.sent   message.sent
    [partition 0]       [partition 1]  [partition 2]
         │                  │              │
    ┌────┴────┐         ┌────┴────┐  ┌────┴────┐
    │          │         │         │  │         │
    ▼          ▼         ▼         ▼  ▼         ▼
    ┌──────────────────────────────────────────┐
    │         Consumer Groups                  │
    ├──────────────────────────────────────────┤
    │                                          │
    │ notification-group  (EmailMessageConsumer)
    │ → Sends email to receiver                │
    │   "You have a new message"               │
    │                                          │
    │ sync-group  (SyncEventConsumer)          │
    │ → Logs to sync_records table             │
    │   (entries for both sender & receiver)   │
    │                                          │
    └──────────────────────────────────────────┘
         │ Email         │ Database Log
         ▼               ▼
    Receiver Email   sync_records
    (via SMTP)       (new rows)
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│             DigitalOcean VPS (Ubuntu 24.04)                │
│             IP: 167.172.99.14                              │
│             2 vCPU, 1.9 GB RAM, 2 GB swap                  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │          Kubernetes (k3s) Cluster                      │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │                                                        │ │
│  │  ┌──────────────────┐  ┌──────────────────┐           │ │
│  │  │ ICTU-Ex Backend  │  │ PostgreSQL Pod   │           │ │
│  │  │ (Spring Boot)    │  │                  │           │ │
│  │  │ Port: 8080       │  │ Port: 5432       │           │ │
│  │  │                  │  │                  │           │ │
│  │  │ Deploys:         │  │ Database:        │           │ │
│  │  │ • auth           │  │ • ictuex         │           │ │
│  │  │ • listing        │  │                  │           │ │
│  │  │ • messaging      │  │ Persistent:      │           │ │
│  │  │ • notification   │  │ /var/lib/postgres           │ │
│  │  │ • sync           │  │                  │           │ │
│  │  └──────────────────┘  └──────────────────┘           │ │
│  │           │                                            │ │
│  │  ┌────────┴──────────┬────────────┬──────────────┐    │ │
│  │  │                   │            │              │    │ │
│  │  ▼                   ▼            ▼              ▼    │ │
│  │ ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │ │  Kafka   │  │  Redis   │  │Prometheus│  │ Grafana  │ │
│  │ │  Pod     │  │  Pod     │  │  Pod     │  │  Pod     │ │
│  │ │9092      │  │ 6379     │  │ 9090     │  │ 3000     │ │
│  │ └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
│  │                                                        │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │    Jenkins CI/CD (Docker Container)                   │ │
│  │    Port: 8080 (different from app)                    │ │
│  │                                                        │ │
│  │  Pipeline:                                            │ │
│  │  1. Git pull → 2. Build → 3. Test                    │ │
│  │  4. Docker build → 5. Deploy to k3s                  │ │
│  │                                                        │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
└─────────────────────────────────────────────────────────────┘
         │
         │ Ingress/Load Balancer
         │
    ┌────▼────────────┐
    │  Nginx Ingress  │  (routes HTTP/S traffic)
    └────────────────┘
         │
    ┌────▼────────────────────────────┐
    │  Clients (Android, Web, etc)    │
    └─────────────────────────────────┘
```

---

## Classification Summary

| Criterion |  System | True Microservices |
|-----------|---------|-------------------|
| Multiple services | ✅ Yes | ✅ Yes |
| Independent deployment | ✅ Possible | ✅ Yes |
| **Separate databases per service** | ❌ **No** | ✅ **Yes** |
| Service owns its schema | ❌ **No** | ✅ **Yes** |
| Eventual consistency required | ❌ No | ✅ Yes |
| Event-driven messaging | ✅ Yes (Kafka) | ✅ Often |
| REST APIs between services | ❌ No | ✅ Often |

### Architecture is:
- ✅ **Service-Oriented (SOA)**
- ✅ **Event-Driven**
- ✅ **Modular Monolith**
- ❌ NOT Microservices (due to shared database)

---

*For more details, see:*
- `README.md` — Project overview and setup
- `KAFKA-TOPICS-AND-SERVICES.md` — Complete event flow documentation
- `ARCHITECTURE-CLASSIFICATION.md` — Deep dive on architecture patterns


