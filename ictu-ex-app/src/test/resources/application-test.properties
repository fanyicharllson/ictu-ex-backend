# Test Configuration for Integration Tests
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Kafka Configuration for Tests
spring.kafka.admin.fail-fast=false
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

# JWT Configuration for Testing
jwt.secret=test-secret-key-for-integration-testing-ictu-ex-2026
jwt.expiration=86400000

# External API Keys (Dummy for Testing)
resend.api.key=dummy-key
gemini.api.key=dummy-key

# Logging
logging.level.root=WARN
logging.level.com.fanyiadrien=INFO

